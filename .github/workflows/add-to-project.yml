name: Add issues/PRs to Project

on:
  issues:
    types: [opened]
  pull_request_target:        
    types: [opened, ready_for_review]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'dependabot[bot]' }}
    steps:
      - uses: actions/add-to-project@v0.6.1
        with:
          project-url: https://github.com/orgs/phys-sims/projects/2
          github-token: ${{ secrets.GITHUB_TOKEN }} # or: ${{ secrets.ADD_TO_PROJECT_PAT }}
